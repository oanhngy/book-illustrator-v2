
- nhận diện user=Header `X-User-Email` --> not authentication
- Đây là bản MVP, only thực hiện requirements P0

**cách chạy dự án**: trong terminal ./start.sh

## Nice-to-have (chưa quyết định làm)
- E2E test bằng Playwright — mô phỏng luồng user thật qua UI. Bỏ qua ở bản đầu (deadline 48h), cân nhắc thêm nếu còn thời gian sau Phase 10.