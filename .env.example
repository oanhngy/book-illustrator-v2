# Gemini API key — dùng từ Phase 5 trở đi (server gọi Gemini REST API).
GEMINI_API_KEY=your-gemini-api-key-here

# true = PipelineService dùng FakeGeminiClient (đọc fixtures/*.json) thay vì gọi API thật.
USE_FAKE_GEMINI=true
